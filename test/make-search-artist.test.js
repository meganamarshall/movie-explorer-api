const test = QUnit.test;

QUnit.module('making url based on search');

test('url includes query and page', assert => {
    //arrange
    const queryOptions = {
        searchInput: 'michael jackson',
        page: 1
    };

    //act

    //assert
});